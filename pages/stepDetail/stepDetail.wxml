
<!-- 步骤条 -->
<i-steps i-class="b-step-wrapper" current="{{ current }}" direction="vertical">
  <block wx:for="{{ stepArray }}" wx:key="item.id">
    <i-step>
      <view slot="title">
        {{ item.title }}
      </view>
    </i-step>
  </block>
</i-steps>